name: Node.js CI (reusable)

on:
  push:
    branches: [main]

jobs:
  call-core-build:
    uses: NoMoreFoodWaste/utility/.github/workflows/reusable-simple-workflow.yml@main
    with:
      name: PIPPO
